#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int i=0;
    int mas[i];
    int max_single=mas[0];
    for(i=0; i<=strlen(mas[i]); ++i){
        if(mas[i]>max_single) max_single=mas[i];
        esle
    }
    return 0;
}
